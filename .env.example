# Local development: SQLite file next to prisma/schema.prisma
DATABASE_URL="file:./dev.db"

# Production (Railway): the file MUST live on the mounted volume at /data,
# otherwise every redeploy wipes the board.
# DATABASE_URL="file:/data/board.db"
